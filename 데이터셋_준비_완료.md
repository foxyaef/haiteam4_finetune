# BDD100K 다운로드 및 학습 준비 완료

2026-09-21 기준, 끊긴 다운로드를 이어받아 원본 ZIP 검증, 이미지 추출, 학습용 변환, 실제 이미지 GPU 추론까지 완료했습니다.

설치 위치:

```text
C:\Users\07lee\OneDrive\바탕 화면\학교\1학년\hai\_pj\_2
```

## 바로 학습 시작

위 폴더에서 PowerShell을 열고 실행하세요. 가상환경 활성화, 데이터 경로 수정, 다운로드·변환 재실행은 필요 없습니다.

```powershell
.\run.cmd train
```

중단했던 실제 학습을 재개할 때는 다음 명령을 사용합니다.

```powershell
.\run.cmd train --resume runs/fp32/last.pth
```

학습은 아직 시작하지 않았습니다. 현재 checkpoint는 공식 COCO 초기 가중치이며, 합성 데이터 테스트 결과는 실제 BDD100K 성능이 아닙니다.

## 준비 및 검증 결과

| 항목 | 결과 |
|---|---|
| 모델 | 공식 RT-DETR v1 R50-vd, COCO pretrained |
| 입력 / 정밀도 | 640×640 / FP32 |
| 학습 이미지 | 공식 train 70,000장 |
| 검증 이미지 | 공식 val 10,000장 |
| 클래스 | Detection 전체 10개 |
| Calibration | seed 42, train에서 고정 512장; val과 겹치지 않음 |
| Train/val 파일명 중복 | 0장 |
| 이미지 검증 | 추출한 80,000장 CRC, 이미지별 SHA256, 이미지 구조 검사 완료 |
| 실제 이미지 모델 점검 | Intel Arc 130V XPU, 입력 1×3×640×640, 10클래스 출력 정상 |
| 변환·평가 테스트 | 6개 통과 |
| 데이터 manifest SHA256 | `93b04dfd5863cabd7f4995289f5798491da0ce7da3e1f0a804f21b9892ea148f` |

기본 학습 설정은 batch 1 / gradient accumulation 8, AdamW, LR 1e-4, 최대 50 epochs입니다. `configs/finetune.yaml`에서 조정할 수 있습니다. validation mAP50:95가 가장 높은 checkpoint를 `runs/fp32/best.pth`로 선택합니다.

학습 완료 후 평가·효율 측정:

```powershell
.\run.cmd evaluate --save-predictions
.\run.cmd benchmark
```

## 사용한 라벨 배포본과 변환

[Berkeley 공식 다운로드 페이지](http://bdd-data.berkeley.edu/download.html)의 **100K Images + 원본 Labels** 배포본을 사용했습니다. **Detection 2020 재라벨링 배포본이 아닙니다.** 확인 당시 Detection 2020 버튼이 라벨 대신 다른 이미지 ZIP으로 연결되어 있어, 공식 서버의 원본 2D bounding box 라벨을 사용했습니다. 다른 모델과 비교할 때도 이 프로젝트와 같은 라벨 release, 변환 JSON, 평가 코드를 사용하세요.

원본 per-image JSON에서 2D box를 추출했고, 공식 train/val 분할과 이미지 전체를 유지했습니다. [Scalabel의 Box2D 변환](https://github.com/scalabel/scalabel/blob/master/scalabel/label/transforms.py)에 따라 끝점을 포함하는 좌표 규칙을 설정에 명시했습니다. 이미지 내부 면적이 전혀 없는 train 박스 1개만 사유를 기록하고 제외했습니다. 해당 이미지 자체와 원본 ZIP은 보존했으며 val 박스는 제외하지 않았습니다.

Test 20,000장은 원본 ZIP에 남겨두었으며 학습·검증에는 사용하지 않습니다. Calibration 512장은 FP32 train에도 포함되며 PTQ에는 동일한 고정 목록을 사용합니다.

## 저장 위치와 재현 기록

아래 경로는 설치 폴더를 기준으로 합니다.

- `data/downloads/`: 원본 이미지·라벨 ZIP, 다운로드 출처·해시, 라이선스
- `data/bdd100k/images/100k/train/`, `val/`: 실제 학습·검증 이미지
- `data/bdd100k/labels/official_raw/`: 정규화한 원본 라벨
- `data/annotations/`: train/val/calibration COCO JSON 및 제외 박스 기록
- `data/calibration_manifest.json`: seed와 고정 512장 목록
- `data/dataset_manifest.json`: 데이터·변환·calibration 해시
- `data/image_sha256.jsonl`: 추출 이미지별 해시
- `reports/dataset_ready.json`: 최종 실제 데이터 점검 결과
- `reports/dataset_download_provenance.json`, `dataset_images_provenance.json`: 원본 출처와 처리 기록
- `reports/environment.json`, `upstream-lock.json`: 실행 환경과 공식 저장소 commit
- `README.md`: 전체 사용법

| 원본 파일 | SHA256 |
|---|---|
| `bdd100k_images_100k.zip` | `afd149eadb49657ef5ad7bb6e9d77cc8b7927a576ee2b475b720028274e02784` |
| `bdd100k_labels.zip` | `7f1f9043c70a6ff0788a323cfb914aeced109a37b7150740d670a347c881394a` |
| `rtdetr_r50vd_6x_coco_from_paddle.pth` | `dfaafa5a3d29958a360c35f1fad553a6f287bfae9967e8ee007e65cff21ddf45` |

공식 저장소 commit: `29320b6fd828f8e0987a71426cf2d961b09dfed7`.

다운로드 임시 조각은 OneDrive 충돌을 피하도록 Windows 임시 폴더에 저장하도록 수정했습니다. 다운로드·검증이 끝난 중복 조각은 정리했고, 최종 원본 ZIP과 실제 데이터는 보존했습니다.

현재 완료 범위는 FP32 파인튜닝 환경과 데이터 준비입니다. INT8/6/4 PTQ, BRECQ, QDrop, sensitivity-aware mixed precision은 아직 구현·실행하지 않았으며, `PTQ_PLAN.md`에 후속 실험 규칙을 정리해 두었습니다.
