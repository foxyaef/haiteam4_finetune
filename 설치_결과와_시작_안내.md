# RT-DETR-R50 로컬 파인튜닝 환경 — 설치 완료

설치 위치:

```text
C:\Users\07lee\OneDrive\바탕 화면\학교\1학년\hai\_pj\_2
```

## 설치·검증한 내용

- Python 3.12.7 독립 가상환경, PyTorch 2.8.0+xpu, torchvision 0.23.0+xpu. 의존성 검사 통과.
- Intel Arc 130V GPU(8GB) 인식 확인. 현재 PC RAM 약 15.54 GiB.
- [공식 RT-DETR v1 R50-vd 모델과 COCO 전용 사전학습 가중치](https://github.com/lyuwenyu/RT-DETR/tree/main/rtdetr_pytorch) 설치.
- 640×640 FP32, 10개 클래스 출력, batch 1 / gradient accumulation 8 기본 설정.
- 클래스 매핑, 비정사각형 좌표 변환, 빈 이미지, 오탐·누락을 포함한 평가 테스트 4개 통과.
- 실제 GPU에서 forward → loss → backward → AdamW update → inference 통과.
- 작은 합성 데이터로 2 epoch 학습, checkpoint 저장·다음 epoch 재개, best validation mAP50:95 선택, 해시 검증 통과.
- Backbone / Encoder / Decoder / Head 파라미터 분류 및 크기·latency·FPS·peak memory 측정 도구 확인.

**BDD100K 데이터는 아직 없으므로 실제 BDD100K 파인튜닝과 정확도 평가는 수행하지 않았습니다.** 생성된 테스트 checkpoint는 합성 입력 검증용입니다. 실제 학습 결과가 아닙니다.

## 다음 할 일

1. [BDD100K 다운로드 페이지](https://bdd-data.berkeley.edu/)에서 **100K Images**와 **Detection 2020 Labels**를 받습니다. 필요한 항목은 [공식 데이터 준비 안내](https://github.com/SysCV/bdd100k-models/blob/main/doc/PREPARE_DATASET.md)에 명시되어 있습니다.
2. 설치 폴더의 `configs/finetune.yaml`에서 `train_images`, `val_images`, `train_labels`, `val_labels`를 실제 위치로 지정합니다. 기본 폴더 구조와 라벨 파일 이름별 예시는 같은 폴더의 `README.md`에 있습니다.
3. PowerShell에서 실행합니다.

```powershell
cd 'C:\Users\07lee\OneDrive\바탕 화면\학교\1학년\hai\_pj\_2'
.\run.cmd prepare
.\run.cmd train
```

`prepare`는 공식 split의 모든 라벨 프레임을 유지하고 COCO 변환·이미지 검사·클래스 매핑·고정 calibration subset 생성을 수행합니다. 변환 결과와 원본 라벨의 해시도 기록합니다.

학습이 끝나면:

```powershell
.\run.cmd evaluate --save-predictions
.\run.cmd benchmark
```

중단한 학습을 마지막 완료 epoch부터 재개하려면:

```powershell
.\run.cmd train --resume runs/fp32/last.pth
```

환경과 합성 학습을 다시 확인하려면:

```powershell
.\run.cmd check
.\run.cmd smoke
```

## 설정·결과 위치

아래 경로는 모두 위 설치 폴더를 기준으로 합니다.

| 파일/폴더 | 용도 |
|---|---|
| README.md | 전체 한국어 사용 설명서, 데이터 배치, 지표 정의 |
| configs/finetune.yaml | 학습·데이터 경로 설정 |
| upstream-lock.json | 공식 repository commit, 초기 가중치 URL/SHA256 |
| reports/environment.json | 최종 설치 환경 및 코드 해시 |
| reports/hardware.json | CPU/GPU/드라이버/RAM 정보 |
| reports/installed-packages.txt | 설치된 전체 라이브러리 버전 |
| reports/unit-tests.log | 데이터·지표 테스트 결과 |
| runs/smoke/smoke_result.json | GPU FP32 640×640 학습 한 단계 검증 |
| runs/integration_test/result.json | 저장·재개·best 선택 통합 검증 |
| reports/module_map.json | 민감도 분석용 파라미터 그룹 |
| runs/fp32/ | 데이터 준비 후 실제 학습 결과가 생성될 위치 |
| PTQ_PLAN.md | 후속 양자화 실험 조건과 미구현 범위 |

실제 학습에서는 `best.pth`, `last.pth`, `history.csv`, epoch별 전체·클래스별 지표, config/seed/환경/해시가 저장됩니다. AP는 0–1 단위이고, Precision/Recall은 confidence 0.5·IoU 0.5에서 계산합니다. Val loss는 최종 출력의 main loss입니다.

## 성능 검증의 범위

합성 테스트에서 최초 FP32 학습 한 단계는 약 16.85초였고, 연산이 준비된 두 번째 epoch에서는 약 1.5초/이미지였습니다. 전체 BDD100K 학습 시간은 이미지별 객체 수·메모리·검증 비용에 따라 달라집니다. 현재 PC에서 전체 학습은 장시간 작업입니다.

벤치마크 도구의 동작 확인은 합성 테스트 checkpoint로 warmup 2회·측정 5회만 실행했습니다. model+postprocess 평균 약 201.9ms, 약 4.95 FPS였습니다. 이는 BDD100K 학습 모델의 최종 성능 측정이 아닙니다. 실제 `best.pth` 생성 후 기본 warmup 10회·측정 50회로 다시 측정하세요. 파일 읽기·이미지 전처리·장치 전송은 측정 범위에서 제외됩니다.

XPU의 grid sampling backward는 완전 결정적 구현이 없어 PyTorch 경고가 발생했습니다. seed, 환경, 코드/가중치 해시를 기록하지만 bitwise 동일 결과까지 보장되지는 않습니다.

## PTQ 준비 상태

train에서 seed 42로 고정된 512장 calibration subset을 만드는 도구와 공통 evaluator, component 분류를 준비했습니다. 실제 subset 파일은 `prepare` 실행 후 생성됩니다. subset은 원래 FP32 train split에도 포함되며 validation과 겹치지 않습니다.

**Basic / MSE·Clipping / BRECQ / QDrop, INT8·INT6·INT4 및 mixed precision 알고리즘은 이번 환경에 구현하지 않았습니다.** 후속 실험 프로토콜을 `PTQ_PLAN.md`에 기록했습니다. FP32 baseline 학습 이후 실제 PTQ integration과 정수 backend 검증이 필요합니다.

공식 소스 commit:

```text
29320b6fd828f8e0987a71426cf2d961b09dfed7
```

초기 checkpoint SHA256:

```text
dfaafa5a3d29958a360c35f1fad553a6f287bfae9967e8ee007e65cff21ddf45
```
